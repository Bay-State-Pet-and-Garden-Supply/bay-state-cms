# ── Dependencies & build ──────────────────────────────────────────────────────
dist/
node_modules
coverage/
*.tsbuildinfo

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
*.code-workspace
.out-of-scope/

# ── Local data & exports ──────────────────────────────────────────────────────
.baystate-cms/
exports/*.zip
exports/*.xml
storage/

# ── Secrets, tokens, environment & databases (security mandate) ───────────────
.baystate-cms-dev-token
.shopsite-cms-dev-token
.shopsite-cms/
.shopsite-cms-dev-token-dir/
.baystate-cms-dev-token-dir/
.env
.env.*
*.db
*.db-shm
*.db-wal

# ── Agent tooling artifacts & working docs ────────────────────────────────────
.agents/*
!.agents/skills/
!.agents/skills/**
# Third-party / local-only skill installs (never committed)
.agents/skills/browser-use/
skills-lock.json
.pi-subagents/
.pi/
.fallow/
.codex/
.impeccable/
HANDOFF.md
worker-handoff.md
.handoff/
pi-session-*.html
scratch/
test-*-workspace/

# ── Parallel-session working docs (excess — never committed) ───────────────
subagents-reports/
.analysis/

# ── Impeccable skill install artifacts (generated on skill upgrade/reinstall) ─
.github/skills/impeccable/scripts/VERSION
.github/skills/impeccable/scripts/bin/
.github/skills/impeccable/scripts/data/
.github/skills/impeccable/scripts/impeccable
.github/skills/impeccable/scripts/impeccable.cmd
.github/skills/impeccable/scripts/live-browser-ignores.js


# ── PI decommission archival dumps (DB data never enters git) ─────────────────
archive/
