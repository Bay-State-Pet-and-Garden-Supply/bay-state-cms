# ── Dependencies & build ──────────────────────────────────────────────────────
dist/
node_modules
coverage/
*.tsbuildinfo

# ── OS / editor noise ─────────────────────────────────────────────────────────
.DS_Store
*.code-workspace
.out-of-scope/

# ── Local data & exports ──────────────────────────────────────────────────────
.baystate-cms/
exports/*.zip
exports/*.xml
storage/

# ── Secrets, tokens, environment & databases (security mandate) ───────────────
.baystate-cms-dev-token
.shopsite-cms-dev-token
.shopsite-cms/
.shopsite-cms-dev-token-dir/
.baystate-cms-dev-token-dir/
.env
.env.*
*.db
*.db-shm
*.db-wal

# ── Agent tooling artifacts & working docs ────────────────────────────────────
.agents/*
!.agents/skills/
!.agents/skills/**
skills-lock.json
.pi-subagents/
.pi/
.fallow/
.codex/
.impeccable/
HANDOFF.md
worker-handoff.md
.handoff/
pi-session-*.html
scratch/
test-*-workspace/

# ── Parallel-session working docs (excess — never committed) ───────────────
subagents-reports/
.analysis/


# ── PI decommission archival dumps (DB data never enters git) ─────────────────
archive/
